{
  "name": "Data Viz Course 2026",
  "image": "mcr.microsoft.com/devcontainers/python:3.10",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },
  "containerEnv": {
    "PATH": "${containerEnv:HOME}/.local/bin:${containerEnv:PATH}"
  },
  "postCreateCommand": "python3 -m pip install --upgrade --user -r requirements.txt",
  "postStartCommand": "command -v streamlit || (echo 'streamlit not found; listing ~/.local/bin:' && ls -la ~/.local/bin || true)",
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "tamasfe.even-better-toml" 
      ]
    }
  },
  "forwardPorts": [8501]
}